{% set classes = ['btn', 'btn-primary'] %}

{% set config = {
    'debug': true,
    'timeout': 30,
    'features': ['cache', 'logging']
} %}

{{ page.slug|replace({'-': '_'}) }}

{% for page in 1..10 %}
    Page {{ page }}
{% endfor %}